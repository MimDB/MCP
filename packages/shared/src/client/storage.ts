/**
 * @module client/storage
 * Domain client for MimDB storage operations (buckets, objects).
 * Implementation is provided by a later task; this export satisfies the
 * {@link MimDBClient} facade's lazy-getter type contract.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export class StorageClient { constructor(_base: any, _ref: string) {} }
