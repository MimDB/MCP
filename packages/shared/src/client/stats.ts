/**
 * @module client/stats
 * Domain client for MimDB observability operations (query stats, logs).
 * Implementation is provided by a later task; this export satisfies the
 * {@link MimDBClient} facade's lazy-getter type contract.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export class StatsClient { constructor(_base: any, _ref: string) {} }
