/**
 * @module client/platform
 * Domain client for MimDB platform/admin operations (orgs, projects, API keys).
 * Implementation is provided by a later task; this export satisfies the
 * {@link MimDBClient} facade's lazy-getter type contract.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export class PlatformClient { constructor(_base: any) {} }
