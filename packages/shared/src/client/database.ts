/**
 * @module client/database
 * Domain client for MimDB database operations (tables, SQL, schema, RLS).
 * Implementation is provided by a later task; this export satisfies the
 * {@link MimDBClient} facade's lazy-getter type contract.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export class DatabaseClient { constructor(_base: any, _ref: string) {} }
