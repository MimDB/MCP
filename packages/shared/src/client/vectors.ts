/**
 * @module client/vectors
 * Domain client for MimDB vector search operations (pgvector tables).
 * Implementation is provided by a later task; this export satisfies the
 * {@link MimDBClient} facade's lazy-getter type contract.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export class VectorsClient { constructor(_base: any, _ref: string) {} }
