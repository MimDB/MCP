/**
 * @module client/cron
 * Domain client for MimDB cron job operations (pg_cron management).
 * Implementation is provided by a later task; this export satisfies the
 * {@link MimDBClient} facade's lazy-getter type contract.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export class CronClient { constructor(_base: any, _ref: string) {} }
