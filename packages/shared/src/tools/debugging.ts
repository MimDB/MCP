/**
 * @module tools/debugging
 * MCP tool definitions for MimDB debugging utilities (query stats, slow queries).
 * @placeholder Implementation is provided by a later task.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function register(..._args: any[]): void {}
